package vn.dongthanh.vsmt.notification.domain;

/** Nhắc nộp / Khiếu nại / Phiếu thu / Thông tin / Giao dịch. */
public enum NotificationKind {
    REMINDER,
    COMPLAINT,
    RECEIPT,
    INFO,
    TRANSACTION
}
