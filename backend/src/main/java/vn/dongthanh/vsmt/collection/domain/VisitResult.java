package vn.dongthanh.vsmt.collection.domain;

/** Vắng nhà / Đã hẹn / Từ chối nộp. Kết quả "đã thu" là Payment, không nằm ở đây. */
public enum VisitResult {
    ABSENT,
    APPOINTMENT,
    REFUSED
}
